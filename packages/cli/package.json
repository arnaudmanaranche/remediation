{
  "name": "remediation",
  "version": "0.10.1",
  "description": "CLI tool that scans React source code and detects design system inconsistencies",
  "license": "MIT",
  "bin": {
    "remediation": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "pnpm run build",
    "changeset:patch": "npm version patch --no-git-tag-version && pnpm run build && npm publish",
    "changeset:minor": "npm version minor --no-git-tag-version && pnpm run build && npm publish",
    "changeset:major": "npm version major --no-git-tag-version && pnpm run build && npm publish"
  },
  "dependencies": {
    "@babel/parser": "^8.0.0",
    "commander": "^12.0.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "knip": "^6.20.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9"
  },
  "keywords": [
    "react",
    "design-system",
    "tokens",
    "lint",
    "code-quality"
  ]
}
