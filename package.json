{
  "name": "remediation-monorepo",
  "version": "0.1.2",
  "description": "CLI tool that scans React source code and detects design system inconsistencies",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "cd packages/cli && npm run build",
    "knip": "knip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:patch": "cd packages/cli && npm run changeset:patch",
    "changeset:minor": "cd packages/cli && npm run changeset:minor",
    "changeset:major": "cd packages/cli && npm run changeset:major"
  }
}
